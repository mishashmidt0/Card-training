export enum ActionTypeForFilter {
  changeIsShowCard = 'FilterReducer/ChangeIsShowCard',
  changePageCount = 'FilterReducer/ChangePageCount',
  changeFilter = 'FilterReducer/changeFilter',
}

export enum AddPackTitle {
  header = 'Add new pack',
  cancel = 'cancel',
  save = 'save',
}

export enum NewPackTitle {
  header = 'Add new pack name',
  cancel = 'cancel',
  save = 'save',
}

export enum ResetTitle {
  button = ' Reset filters',
}

export enum FilterText {
  all = 'all',
  my = 'my',
  header = 'Show packs cards',
}

export enum Star {
  star0,
  star1,
  star2,
  star3,
  star4,
  star5,
}
